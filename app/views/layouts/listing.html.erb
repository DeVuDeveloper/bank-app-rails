<div class="card border-info">
  <div class="card-header shadow-sm bg-dark text-white text-center">
    <%= yield :heading %>
  </div>

  <div class="card-body p-0">
    <%= yield :body %>
  </div>

  <div class="card-footer bg-dark text-white text-center">
    <%= yield :footer %>
  </div>
</div>

<%= yield :after %>
