<% limit ||= 3 %>
<% name_field ||= :name %>
<% separator ||= false %>
<% view_all ||= root_path %>
<% if separator %>
  <% items.limit(limit).each_with_index do |item, index| %>
    <%= separator unless index.zero? %>
    <%= link_to item.send(name_field), item %>
  <% end %>
<% else %>
  <ul class="list-unstyled mb-0">
    <% items.limit(limit).each do |item| %>
      <li><%= link_to item.send(name_field), item %></li>
    <% end %>
  </ul>
<% end %>
<% if items.empty? %>
  None
<% elsif items.size > 3 %>
  <%= separator %>
  &hellip;
  <br />
  共 <%= items.size %> 个，<%= link_to 'View All', view_all %>
<% end %>
